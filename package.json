{
  "name": "rec360_gallery",
  "version": "0.0.1",
  "description": "",
  "keywords": [
    "gallery",
    "carousel",
    "modal"
  ],
  "license": "",
  "author": "",
  "scripts": {
    "build": "parcel build index.html --public-url ./",
    "dev": "parcel index.html --open",
    "start": "cp -R img dist/ && npm run build && npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "eslint-plugin-html": "^6.2.0",
    "parcel-bundler": "^1.12.4",
    "standard": "^16.0.4"
  }
}
